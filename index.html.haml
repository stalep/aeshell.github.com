---
layout: base
---

.row
  .span7
    .page-header
      %h3 Æsh
      %br
      %p Æsh (Another Extendable SHell) is a Java library that provides an API which allow the programmer to write command-based programs in a terminal-independent manner. Æsh support most GNU Readline features and many different terminal operations.

    .step
      %h2 Features:
      %ul
        %li Line editing
        %li History (search, persistence)
        %li Completion
        %li Masking
        %li Undo and Redo
        %li Paste buffer
        %li Emacs and Vi editing mode
        %li Support POSIX OS's and Windows
        %li Support out and error streams
        %li Command line parser
        %li Aliases
        %li Redirect
        %li.last Pipeline

      %p - and its very configurable

    -#.step
      -#%h2 some headling2 alskdfjaølj 
      -#%p some text....

  .span5
    .hero-unit
      %h2 Latest Release
      %p <b>Version 0.36: </b>
      %lu
        %li Input read is threaded
        %li Persisting aliases
        %li CompletionParser
        %li ++
      %p
      %p <a href="/docs/base/">Grab</a> it while its hot!
      -#- for post in site.posts
        -#%h3
          -#= post.date.strftime( '%d %B %Y' )
          -#,
          -#= post.title
        -#.content
          -#= summarize(html_to_text(post.content), 50)
        -#%p


      -#%p
        -#This site is created with
        -#%a{:href=>'http://awestruct.org'} awestruct
      -#%p
        -#How cool is that?

    -#%section
      -#%h2 About
      -#%p
        -#asldj asldkfj asldfkj 
    -#%section
      -#%h2 Goal
      -#%p
        -#the goal is to...
.row
  .span8
    %section
      %h2 News
      %p
      - for post in site.posts
        %h3
          = post.date.strftime( '%d %B %Y' )
          ,
          = post.title
        .content
          = post.content
        %p


